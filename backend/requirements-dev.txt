# 테스트 전용 의존성 — CI·로컬 pytest용. 런타임(requirements.txt)을 포함한다.
# 지금까지 로컬 .venv에만 설치돼 있어 CI에서 pytest not found(exit 127)가 났다.
-r requirements.txt
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
respx==0.23.1
