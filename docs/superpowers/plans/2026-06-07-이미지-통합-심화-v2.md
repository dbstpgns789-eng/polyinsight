# P2 이미지 통합 심화 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** statement·closing_v2 뼈대에 "담긴 존" 이미지를 추가하고, focal(클릭 초점) 제어를 end-to-end로 배선한다.

**Architecture:** 모델 A(디자인된 역할 고정). 이미지는 둥근 존에만 담기고 텍스트는 절대 겹치지 않는다(readability 보호). 무이미지 시 텍스트가 자리를 채우는 적응 분기(`showImage = !!card.image_url || mode==='edit'`)는 Cover/Feature 패턴을 재사용. focal은 `Card.focal{x,y}`로 저장하고, 편집 모드에서 이미지 본문 클릭으로 설정(우상단 ↻는 교체).

**Tech Stack:** Next.js 15 + TypeScript + React, Vitest 3.2.6(순수 로직), 시각 검증은 `/render/demo/*` Playwright 캡처. 백엔드 변경 0.

**선행 문서:** `docs/superpowers/specs/2026-06-07-이미지-통합-심화-v2-design.md`

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `web/src/lib/focal.ts` | focal 순수 유틸(클램프·클릭→focal·objectPosition) | **신규** |
| `web/src/lib/focal.test.ts` | focal 유틸 단위 테스트 | **신규** |
| `web/src/types/editor.ts` | `Card.focal?` 필드 | 수정 |
| `web/src/lib/imageSlots.ts` | `statement`/`closing_v2` → `'zone'` | 수정 |
| `web/src/lib/imageSlots.test.ts` | 슬롯 매핑 테스트 | **신규** |
| `web/src/components/cards/types.ts` | `CardComponentProps.onFocalChange?` | 수정 |
| `web/src/components/cards/shared/EditableImage.tsx` | 본문클릭=초점 + 우상단↻교체 + 십자표식 | 수정 |
| `web/src/components/cards/skin/VisualZone.tsx` | focal/onFocalChange 통과 | 수정 |
| `web/src/components/cards/skeletons/Cover.tsx` | focal 배선 | 수정 |
| `web/src/components/cards/skeletons/Feature.tsx` | focal 배선 | 수정 |
| `web/src/components/cards/skeletons/Statement.tsx` | 하단 밴드 VisualZone + 적응 + focal | 수정 |
| `web/src/components/cards/skeletons/Closing.tsx` | 하단 중앙 밴드 VisualZone + 적응 + focal | 수정 |
| `web/src/app/editor/[jobId]/page.tsx` | `handleFocalUpdate` + 배선 | 수정 |
| `web/src/components/editor/MidCanvas.tsx` | `onFocalChange` 통과 | 수정 |

모든 테스트/명령은 `web/`에서 실행: `cd web` 후 `npm test` 또는 `npx vitest run <file>`.

---

### Task 1: focal 순수 유틸 (TDD)

**Files:**
- Create: `web/src/lib/focal.ts`
- Test: `web/src/lib/focal.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// web/src/lib/focal.test.ts
import { describe, it, expect } from 'vitest'
import { clampFocal, clickToFocal, focalToObjectPosition } from './focal'

describe('clampFocal', () => {
  it('0~1 범위로 클램프', () => {
    expect(clampFocal({ x: -0.5, y: 1.7 })).toEqual({ x: 0, y: 1 })
  })
  it('범위 안 값은 보존', () => {
    expect(clampFocal({ x: 0.3, y: 0.8 })).toEqual({ x: 0.3, y: 0.8 })
  })
})

describe('clickToFocal', () => {
  it('클릭 좌표를 rect 기준 비율로 변환', () => {
    const rect = { left: 100, top: 200, width: 400, height: 400 }
    expect(clickToFocal(300, 400, rect)).toEqual({ x: 0.5, y: 0.5 })
  })
  it('rect 밖 클릭은 클램프', () => {
    const rect = { left: 0, top: 0, width: 200, height: 200 }
    expect(clickToFocal(-50, 250, rect)).toEqual({ x: 0, y: 1 })
  })
})

describe('focalToObjectPosition', () => {
  it('undefined면 center', () => {
    expect(focalToObjectPosition(undefined)).toBe('center')
  })
  it('focal을 퍼센트 문자열로', () => {
    expect(focalToObjectPosition({ x: 0.25, y: 0.75 })).toBe('25% 75%')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd web && npx vitest run src/lib/focal.test.ts`
Expected: FAIL — `Failed to resolve import './focal'`

- [ ] **Step 3: Write minimal implementation**

```ts
// web/src/lib/focal.ts
// 이미지 초점(focal point) 순수 유틸. {x,y}는 0~1 정규화 좌표.

export interface Focal {
  x: number
  y: number
}

function clamp01(n: number): number {
  return Math.min(1, Math.max(0, n))
}

export function clampFocal(f: Focal): Focal {
  return { x: clamp01(f.x), y: clamp01(f.y) }
}

interface Rect {
  left: number
  top: number
  width: number
  height: number
}

/** 클릭 좌표(clientX/Y)를 요소 rect 기준 0~1 focal로 변환(클램프 포함). */
export function clickToFocal(clientX: number, clientY: number, rect: Rect): Focal {
  return clampFocal({
    x: (clientX - rect.left) / rect.width,
    y: (clientY - rect.top) / rect.height,
  })
}

/** focal → CSS objectPosition 문자열. 없으면 'center'. */
export function focalToObjectPosition(focal?: Focal): string {
  if (!focal) return 'center'
  const { x, y } = clampFocal(focal)
  return `${x * 100}% ${y * 100}%`
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd web && npx vitest run src/lib/focal.test.ts`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add web/src/lib/focal.ts web/src/lib/focal.test.ts
git commit -m "[WEB] focal 순수 유틸 — 클램프·클릭변환·objectPosition (TDD)"
```

---

### Task 2: Card.focal 타입 + imageSlots 2키 (TDD)

**Files:**
- Modify: `web/src/types/editor.ts:10-15`
- Modify: `web/src/lib/imageSlots.ts:10-25`
- Test: `web/src/lib/imageSlots.test.ts` (create)

- [ ] **Step 1: Write the failing test**

```ts
// web/src/lib/imageSlots.test.ts
import { describe, it, expect } from 'vitest'
import { getSlotType } from './imageSlots'

describe('getSlotType — P2 신규 존 뼈대', () => {
  it('statement는 zone', () => {
    expect(getSlotType('statement')).toBe('zone')
  })
  it('closing_v2는 zone', () => {
    expect(getSlotType('closing_v2')).toBe('zone')
  })
  it('데이터 4종은 none 유지', () => {
    expect(getSlotType('bigstat_compare')).toBe('none')
    expect(getSlotType('process_v2')).toBe('none')
    expect(getSlotType('reasons')).toBe('none')
    expect(getSlotType('grid_v2')).toBe('none')
  })
  it('기존 cover_v2/feature는 zone 유지', () => {
    expect(getSlotType('cover_v2')).toBe('zone')
    expect(getSlotType('feature')).toBe('zone')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd web && npx vitest run src/lib/imageSlots.test.ts`
Expected: FAIL — `statement는 zone` 기대 'zone' 받음 'none' (closing_v2도 동일)

- [ ] **Step 3: Write minimal implementation**

`web/src/lib/imageSlots.ts`의 `IMAGE_SLOT_TYPES`에서 `cover_v2`/`feature` 줄 뒤에 두 줄 추가:

```ts
  cover_v2:   'zone',
  feature:    'zone',
  statement:  'zone',
  closing_v2: 'zone',
  data:       'none',
  brand:      'none',
```

`web/src/types/editor.ts`의 `Card` 인터페이스에 `focal` 추가:

```ts
export interface Card {
  card_num: number
  template_type: string
  image_url?: string
  focal?: { x: number; y: number }
  fields?: Record<string, FieldValue>
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd web && npx vitest run src/lib/imageSlots.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add web/src/types/editor.ts web/src/lib/imageSlots.ts web/src/lib/imageSlots.test.ts
git commit -m "[WEB] Card.focal 필드 + statement/closing_v2 이미지 존 등록 (TDD)"
```

---

### Task 3: CardComponentProps에 onFocalChange 추가

**Files:**
- Modify: `web/src/components/cards/types.ts:7-15`

- [ ] **Step 1: 타입 추가**

`CardComponentProps`에 `onFocalChange` 한 줄 추가:

```ts
export interface CardComponentProps {
  card: Card
  theme: CardTheme
  mode: CardMode
  onFieldChange?: (fieldKey: string, value: string) => void
  onImageRequest?: (slotKey: string) => void
  onFocalChange?: (focal: { x: number; y: number }) => void
  onFieldFocus?: (fieldKey: string) => void
  focusedField?: string | null
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd web && npx tsc --noEmit`
Expected: 통과 (선택적 prop이라 기존 사용처 영향 없음)

- [ ] **Step 3: Commit**

```bash
git add web/src/components/cards/types.ts
git commit -m "[WEB] CardComponentProps에 onFocalChange 추가"
```

---

### Task 4: EditableImage — 클릭 초점 + 우상단 ↻ 교체 + 십자 표식

**Files:**
- Modify: `web/src/components/cards/shared/EditableImage.tsx` (이미지 있음 분기 전체 교체; 빈 슬롯 분기는 유지)

기존: 이미지 전체가 "교체" 버튼이라 클릭 초점과 충돌. 본문 클릭=초점, 우상단 작은 ↻=교체로 분리.

- [ ] **Step 1: 헤더 import + props 교체**

파일 상단 import와 `EditableImageProps`를 다음으로 교체:

```tsx
'use client'

import { type CSSProperties, type MouseEvent, useState } from 'react'
import { clickToFocal, focalToObjectPosition, type Focal } from '@/lib/focal'

type Mode = 'edit' | 'render' | 'thumbnail'

interface EditableImageProps {
  imageUrl?: string
  slotKey: string
  mode: Mode
  alt?: string
  className?: string
  style?: CSSProperties
  objectFit?: 'cover' | 'contain'
  focal?: Focal
  onImageRequest?: (slotKey: string) => void
  onFocalChange?: (focal: Focal) => void
}
```

- [ ] **Step 2: 함수 시그니처 + 이미지 있음 분기 교체**

`export default function EditableImage({...})` 시그니처를 새 props로 바꾸고, `if (imageUrl) { ... }` 블록 전체를 교체:

```tsx
export default function EditableImage({
  imageUrl,
  slotKey,
  mode,
  alt = '',
  className,
  style,
  objectFit = 'cover',
  focal,
  onImageRequest,
  onFocalChange,
}: EditableImageProps) {
  const [hovered, setHovered] = useState(false)
  const isEditable = mode === 'edit'
  const objectPosition = focalToObjectPosition(focal)

  if (imageUrl) {
    const handleFocalClick = (e: MouseEvent<HTMLDivElement>) => {
      if (!isEditable || !onFocalChange) return
      const rect = e.currentTarget.getBoundingClientRect()
      onFocalChange(clickToFocal(e.clientX, e.clientY, rect))
    }

    return (
      <div
        className={className}
        style={{
          position: 'relative',
          width: '100%',
          height: '100%',
          cursor: isEditable ? 'crosshair' : 'default',
          ...style,
        }}
        onMouseEnter={isEditable ? () => setHovered(true) : undefined}
        onMouseLeave={isEditable ? () => setHovered(false) : undefined}
        onClick={handleFocalClick}
      >
        <img
          src={imageUrl}
          alt={alt}
          style={{ width: '100%', height: '100%', objectFit, objectPosition, display: 'block' }}
        />

        {isEditable && (
          <>
            {/* 현재 초점 십자 표식 (클릭 통과) */}
            <div
              style={{
                position: 'absolute',
                left: `${(focal?.x ?? 0.5) * 100}%`,
                top: `${(focal?.y ?? 0.5) * 100}%`,
                transform: 'translate(-50%, -50%)',
                width: 26,
                height: 26,
                borderRadius: '50%',
                border: '2px solid rgba(255, 255, 255, 0.95)',
                boxShadow: '0 0 0 2px rgba(0, 0, 0, 0.45)',
                pointerEvents: 'none',
              }}
            />
            {/* 우상단 교체 버튼 (stopPropagation으로 초점과 분리) */}
            <button
              type="button"
              onClick={(e) => { e.stopPropagation(); onImageRequest?.(slotKey) }}
              aria-label="이미지 교체"
              style={{
                position: 'absolute',
                top: 10,
                right: 10,
                width: 36,
                height: 36,
                borderRadius: 8,
                background: hovered ? 'rgba(0, 0, 0, 0.65)' : 'rgba(0, 0, 0, 0.45)',
                color: '#fff',
                border: 'none',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                cursor: 'pointer',
                transition: 'background 150ms ease',
              }}
            >
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <polyline points="23 4 23 10 17 10" />
                <polyline points="1 20 1 14 7 14" />
                <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15" />
              </svg>
            </button>
          </>
        )}
      </div>
    )
  }

  // 빈 슬롯 (edit 모드만 표시; render/thumbnail은 null) — 기존 유지
```

빈 슬롯 분기(`if (!isEditable) return null` 이하)는 그대로 둔다. `onImageRequest?.(slotKey)` 호출은 변경 없음.

- [ ] **Step 3: 타입 체크**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

- [ ] **Step 4: Commit**

```bash
git add web/src/components/cards/shared/EditableImage.tsx
git commit -m "[WEB] EditableImage 클릭초점 + 우상단↻교체 분리 + 초점 십자표식"
```

---

### Task 5: VisualZone — focal/onFocalChange 통과

**Files:**
- Modify: `web/src/components/cards/skin/VisualZone.tsx` (전체 교체)

VisualZone이 직접 objectPosition을 계산하던 것을 EditableImage로 위임(단일 소스). focal·onFocalChange를 통과.

- [ ] **Step 1: 전체 교체**

```tsx
// VisualZone — 디자인된 이미지 존. 둥근 프레임 + 표면 플레이스홀더.
// focal/onFocalChange는 EditableImage로 위임(클릭 초점).
import EditableImage from '../shared/EditableImage'
import type { Focal } from '@/lib/focal'

interface VisualZoneProps {
  imageUrl?: string
  slotKey: string
  mode: 'edit' | 'render' | 'thumbnail'
  focal?: Focal
  radius?: boolean       // 둥근 모서리(기본 true)
  onImageRequest?: (slotKey: string) => void
  onFocalChange?: (focal: Focal) => void
}

export default function VisualZone({
  imageUrl, slotKey, mode, focal, radius = true, onImageRequest, onFocalChange,
}: VisualZoneProps) {
  return (
    <div style={{
      width: '100%', height: '100%', overflow: 'hidden',
      borderRadius: radius ? 'var(--set-radius-box)' : 0,
      background: 'var(--set-surface)',
    }}>
      <EditableImage
        imageUrl={imageUrl}
        slotKey={slotKey}
        mode={mode}
        objectFit="cover"
        focal={focal}
        onImageRequest={onImageRequest}
        onFocalChange={onFocalChange}
      />
    </div>
  )
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

- [ ] **Step 3: Commit**

```bash
git add web/src/components/cards/skin/VisualZone.tsx
git commit -m "[WEB] VisualZone focal/onFocalChange 통과 (objectPosition은 EditableImage로 위임)"
```

---

### Task 6: Cover/Feature — 기존 VisualZone에 focal 배선

**Files:**
- Modify: `web/src/components/cards/skeletons/Cover.tsx:8,26`
- Modify: `web/src/components/cards/skeletons/Feature.tsx:8,27`

- [ ] **Step 1: Cover 배선**

`Cover.tsx`의 props 구조분해에 `onFocalChange` 추가, VisualZone 호출에 focal·onFocalChange 추가:

```tsx
  const { card, mode, onFieldChange, onFieldFocus, focusedField, onImageRequest, onFocalChange } = props
```

```tsx
            <VisualZone imageUrl={card.image_url} slotKey="cover" mode={mode} focal={card.focal} onImageRequest={onImageRequest} onFocalChange={onFocalChange} />
```

- [ ] **Step 2: Feature 배선**

`Feature.tsx`도 동일하게 props에 `onFocalChange` 추가, VisualZone 호출 교체:

```tsx
  const { card, mode, onFieldChange, onFieldFocus, focusedField, onImageRequest, onFocalChange } = props
```

```tsx
            <VisualZone imageUrl={card.image_url} slotKey="feature" mode={mode} focal={card.focal} onImageRequest={onImageRequest} onFocalChange={onFocalChange} />
```

- [ ] **Step 3: 타입 체크**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

- [ ] **Step 4: Commit**

```bash
git add web/src/components/cards/skeletons/Cover.tsx web/src/components/cards/skeletons/Feature.tsx
git commit -m "[WEB] Cover/Feature focal 배선 (card.focal → VisualZone)"
```

---

### Task 7: Statement — 하단 이미지 밴드 + 적응 + focal

**Files:**
- Modify: `web/src/components/cards/skeletons/Statement.tsx` (전체 교체)

텍스트는 위, 이미지는 하단 밴드. 이미지 없으면 텍스트가 세로 중앙으로 채움(빈 박스 0).

- [ ] **Step 1: 전체 교체**

```tsx
// ② Statement — beat2 훅 / beat3 한계. 큰 질문 + 본문 + (선택) 하단 이미지 밴드.
'use client'
import { CardSurface, BrandMark, Eyebrow, Headline, Body, VisualZone } from '../skin'
import type { CardComponentProps } from '../types'
import { fieldValue } from '../types'

export default function Statement(props: CardComponentProps) {
  const { card, mode, onFieldChange, onFieldFocus, focusedField, onImageRequest, onFocalChange } = props
  const showImage = !!card.image_url || mode === 'edit'
  return (
    <CardSurface>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
        <Eyebrow value={fieldValue(card, 'eyebrow')} fieldKey="eyebrow" mode={mode}
          onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'eyebrow'} />
        <BrandMark />
      </div>
      {showImage ? (
        <>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 28, marginTop: 40 }}>
            <Headline value={fieldValue(card, 'headline')} fieldKey="headline" mode={mode}
              onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'headline'} />
            <Body value={fieldValue(card, 'body')} fieldKey="body" mode={mode}
              onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'body'} />
          </div>
          <div style={{ flex: 1, minHeight: 0, marginTop: 32 }}>
            <VisualZone imageUrl={card.image_url} slotKey="statement" mode={mode} focal={card.focal}
              onImageRequest={onImageRequest} onFocalChange={onFocalChange} />
          </div>
        </>
      ) : (
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: 32 }}>
          <Headline value={fieldValue(card, 'headline')} fieldKey="headline" mode={mode}
            onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'headline'} />
          <Body value={fieldValue(card, 'body')} fieldKey="body" mode={mode}
            onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'body'} />
        </div>
      )}
    </CardSurface>
  )
}
```

- [ ] **Step 2: 타입 체크 + 강제규칙 grep**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

Run: `cd web && grep -nE "background|color:|fontSize|border" src/components/cards/skeletons/Statement.tsx`
Expected: 매치 0건 (뼈대는 스타일 토큰을 직접 쓰지 않음 — 레이아웃 속성만)

- [ ] **Step 3: Commit**

```bash
git add web/src/components/cards/skeletons/Statement.tsx
git commit -m "[WEB] Statement 하단 이미지 밴드 + 무이미지 적응 + focal"
```

---

### Task 8: Closing — 하단 중앙 이미지 밴드 + 적응 + focal

**Files:**
- Modify: `web/src/components/cards/skeletons/Closing.tsx` (전체 교체)

텍스트 블록 아래, 출처 태그 위에 하단 중앙 이미지 밴드(`maxWidth`로 중앙 정렬). 이미지 없으면 기존 세로 중앙 레이아웃 유지(빈 박스 0).

- [ ] **Step 1: 전체 교체**

```tsx
// ⑧ Closing — beat9(마무리/협력). 텍스트 + (선택) 하단 중앙 이미지 밴드 + 출처.
'use client'
import { CardSurface, BrandMark, Eyebrow, Headline, Body, SourceTag, AccentDivider, VisualZone } from '../skin'
import type { CardComponentProps } from '../types'
import { fieldValue } from '../types'

export default function Closing(props: CardComponentProps) {
  const { card, mode, onFieldChange, onFieldFocus, focusedField, onImageRequest, onFocalChange } = props
  const showImage = !!card.image_url || mode === 'edit'
  return (
    <CardSurface>
      <div style={{ display: 'flex', justifyContent: 'flex-end' }}><BrandMark /></div>
      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: 24 }}>
        <Eyebrow value={fieldValue(card, 'eyebrow')} fieldKey="eyebrow" mode={mode}
          onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'eyebrow'} />
        <Headline value={fieldValue(card, 'headline')} fieldKey="headline" mode={mode}
          onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'headline'} />
        <AccentDivider />
        <Body value={fieldValue(card, 'body')} fieldKey="body" mode={mode}
          onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'body'} />
        {showImage && (
          <div style={{ width: '100%', maxWidth: 620, alignSelf: 'center', aspectRatio: '16 / 7', marginTop: 8 }}>
            <VisualZone imageUrl={card.image_url} slotKey="closing_v2" mode={mode} focal={card.focal}
              onImageRequest={onImageRequest} onFocalChange={onFocalChange} />
          </div>
        )}
      </div>
      <SourceTag value={fieldValue(card, 'source_ref')} fieldKey="source_ref" mode={mode}
        onFieldChange={onFieldChange} onFieldFocus={onFieldFocus} focused={focusedField === 'source_ref'} />
    </CardSurface>
  )
}
```

- [ ] **Step 2: 타입 체크 + 강제규칙 grep**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

Run: `cd web && grep -nE "background|color:|fontSize|border" src/components/cards/skeletons/Closing.tsx`
Expected: 매치 0건 (레이아웃 속성 width/maxWidth/aspectRatio/margin/gap만)

- [ ] **Step 3: Commit**

```bash
git add web/src/components/cards/skeletons/Closing.tsx
git commit -m "[WEB] Closing 하단 중앙 이미지 밴드 + 무이미지 적응 + focal"
```

---

### Task 9: editor + MidCanvas — handleFocalUpdate 배선

**Files:**
- Modify: `web/src/app/editor/[jobId]/page.tsx` (handleFocalUpdate 추가 + MidCanvas에 전달)
- Modify: `web/src/components/editor/MidCanvas.tsx` (onFocalChange prop 추가 + CardRenderer로 전달)

- [ ] **Step 1: editor에 handleFocalUpdate 추가**

`page.tsx`의 `handleImageUpdate` 정의(86~98행) 바로 아래에 추가:

```tsx
  const handleFocalUpdate = useCallback((focal: { x: number; y: number }) => {
    setLocalData((prev) => {
      const base = prev ?? apiData?.cardData
      if (!base) return prev
      const updatedCards = base.cards.map((card, idx) => {
        if (idx !== activeCardIdx) return card
        return { ...card, focal }
      })
      const updated = { ...base, cards: updatedCards }
      debouncedSave(updated)
      return updated
    })
  }, [activeCardIdx, apiData, debouncedSave])
```

- [ ] **Step 2: MidCanvas 호출에 prop 전달**

`page.tsx`의 `<MidCanvas ... onImageUploadRequest={...} />`에 한 줄 추가:

```tsx
          onImageUploadRequest={() => setImageUploadRequested(true)}
          onFocalChange={handleFocalUpdate}
```

- [ ] **Step 3: MidCanvas Props + 전달**

`MidCanvas.tsx`의 `interface Props`에 추가:

```tsx
  onImageUploadRequest?: () => void
  onFocalChange?: (focal: { x: number; y: number }) => void
```

구조분해에 `onFocalChange` 추가:

```tsx
  onFieldFocus, onFieldChange, onImageUploadRequest, onFocalChange,
```

`<CardRenderer ... onImageRequest={...} />`에 한 줄 추가:

```tsx
            onImageRequest={onImageUploadRequest ? () => onImageUploadRequest() : undefined}
            onFocalChange={onFocalChange}
```

- [ ] **Step 4: 타입 체크**

Run: `cd web && npx tsc --noEmit`
Expected: 통과

- [ ] **Step 5: Commit**

```bash
git add web/src/app/editor/[jobId]/page.tsx web/src/components/editor/MidCanvas.tsx
git commit -m "[WEB] editor handleFocalUpdate + MidCanvas onFocalChange 배선 (end-to-end)"
```

---

### Task 10: 시각 게이트 — 빌드 + 데모 캡처 검증

**Files:** 없음 (검증 전용). 완료 기준은 "빌드 성공"이 아니라 **브라우저 시각 일치**(CLAUDE.md §13).

- [ ] **Step 1: 전체 테스트 + 빌드**

Run: `cd web && npm test && npx tsc --noEmit`
Expected: focal(6) + imageSlots(4) + 기존 parse/delimiters 테스트 모두 PASS, tsc 클린

- [ ] **Step 2: 데모 카드에 statement/closing_v2 + 이미지 유무 케이스 확인**

`web/src/app/render/demo/` 의 데모 데이터에 statement·closing_v2 카드가 있는지 확인하고, 다음을 직접 캡처(Playwright 또는 브라우저)하여 검증:
- statement **이미지 있음**: 텍스트 위 + 하단 밴드 사진, 텍스트가 사진 위에 겹치지 않음
- statement **이미지 없음**(image_url 제거): 빈 박스 없이 텍스트가 세로 중앙으로 채움
- closing_v2 **이미지 있음**: 하단 중앙 밴드, 출처 태그는 그 아래 유지
- 에디터(`/editor/demo`)에서 statement/closing 선택 시 RightPanel에 이미지 업로드 UI 활성(`getSlotType==='zone'`), 캔버스 이미지 본문 클릭 시 십자 표식 이동 + 우상단 ↻ 표시

검증 기준: 흰박스 0, 사진 위 텍스트 0, 에디토리얼 라이트 팔레트 일관.

- [ ] **Step 3: 강제규칙 최종 grep**

Run: `cd web && grep -rnE "background:|color:|fontSize|borderRadius" src/components/cards/skeletons/Statement.tsx src/components/cards/skeletons/Closing.tsx`
Expected: 레이아웃 외 스타일 토큰 0건(존 둥근모서리는 VisualZone이 `--set-radius-box`로 담당, 뼈대는 레이아웃만)

- [ ] **Step 4: 검증 결과 기록 후 커밋(문서/메모리)**

시각 게이트 통과 여부를 사용자에게 보고. 통과 시 브랜치 머지 여부는 사용자 판단(`finishing-a-development-branch`).

---

### Task 11: 채움/전체 맞춤 토글 (spec §2.6 — 추가)

focal만으론 비-비율 이미지가 잘려 외부 사전편집이 필요한 번거로움. `Card.image_fit`
('cover'|'contain', 기본 cover)로 고정 존 안에서 이미지 앉는 방식을 선택. contain=통째로.
focal과 동형 배선.

**Files:**
- Modify: `web/src/types/editor.ts` (`Card.image_fit?`)
- Modify: `web/src/components/cards/types.ts` (`CardComponentProps.onFitChange?`)
- Modify: `web/src/components/cards/shared/EditableImage.tsx` (좌상단 토글 + cover에서만 focal)
- Modify: `web/src/components/cards/skin/VisualZone.tsx` (fit/onFitChange 통과)
- Modify: `web/src/components/cards/skeletons/Cover.tsx`·`Feature.tsx`·`Statement.tsx`·`Closing.tsx` (fit 배선)
- Modify: `web/src/app/editor/[jobId]/page.tsx` (`handleFitUpdate`)
- Modify: `web/src/components/editor/MidCanvas.tsx` (`onFitChange` 통과)

- [ ] **Step 1: 데이터 모델 + props 타입**

`web/src/types/editor.ts`의 `Card`에 추가:
```ts
  image_url?: string
  focal?: { x: number; y: number }
  image_fit?: 'cover' | 'contain'
```

`web/src/components/cards/types.ts`의 `CardComponentProps`에 `onFocalChange` 다음 줄 추가:
```ts
  onFocalChange?: (focal: { x: number; y: number }) => void
  onFitChange?: (fit: 'cover' | 'contain') => void
```

- [ ] **Step 2: EditableImage — objectFit 토글 버튼 + cover 전용 focal**

`web/src/components/cards/shared/EditableImage.tsx`:
- `EditableImageProps`에 `onFitChange?: (fit: 'cover' | 'contain') => void` 추가(기존 `objectFit?: 'cover' | 'contain'` prop은 유지 — 구 템플릿 호환).
- 함수 시그니처 구조분해에 `onFitChange` 추가.
- `if (imageUrl)` 블록에서:
  - `objectPosition`을 cover에서만 적용: 기존 `const objectPosition = focalToObjectPosition(focal)`를
    `const objectPosition = objectFit === 'cover' ? focalToObjectPosition(focal) : 'center'`로 변경.
  - focal 클릭 가드를 cover로 한정: `handleFocalClick` 안 `if (!isEditable || !onFocalChange) return`를
    `if (!isEditable || !onFocalChange || objectFit !== 'cover') return`로.
  - 십자 표식 렌더 조건을 cover로 한정: `{isEditable && (` 내부 십자 `<div>`를 `{isEditable && objectFit === 'cover' && (...)}`로 감싼다(교체 버튼은 그대로).
  - 좌상단 토글 버튼 추가(교체 버튼과 형제, `onFitChange` 있을 때만):
```tsx
            {isEditable && onFitChange && (
              <button
                type="button"
                onClick={(e) => { e.stopPropagation(); onFitChange(objectFit === 'cover' ? 'contain' : 'cover') }}
                aria-label="이미지 맞춤 전환"
                style={{
                  position: 'absolute',
                  top: 10,
                  left: 10,
                  height: 36,
                  padding: '0 12px',
                  borderRadius: 8,
                  background: hovered ? 'rgba(0, 0, 0, 0.65)' : 'rgba(0, 0, 0, 0.45)',
                  color: '#fff',
                  border: 'none',
                  fontSize: 13,
                  fontWeight: 700,
                  cursor: 'pointer',
                  transition: 'background 150ms ease',
                }}
              >
                {objectFit === 'cover' ? '채움' : '전체'}
              </button>
            )}
```
  (이 토글 버튼은 십자 표식 `<div>`와 교체 `<button>`이 들어있는 `<>...</>` 안, 교체 버튼 옆에 둔다.)

- [ ] **Step 3: VisualZone — fit/onFitChange 통과**

`web/src/components/cards/skin/VisualZone.tsx`의 `VisualZoneProps`에 추가하고 EditableImage로 전달:
```tsx
  focal?: Focal
  fit?: 'cover' | 'contain'
  radius?: boolean
  onImageRequest?: (slotKey: string) => void
  onFocalChange?: (focal: Focal) => void
  onFitChange?: (fit: 'cover' | 'contain') => void
```
구조분해에 `fit, ... onFitChange` 추가. `<EditableImage>`의 `objectFit="cover"`를
`objectFit={fit ?? 'cover'}`로 바꾸고 `onFitChange={onFitChange}` 추가.

- [ ] **Step 4: 4개 뼈대 fit 배선**

Cover·Feature·Statement·Closing 각각:
- props 구조분해에 `onFitChange` 추가.
- 각 `<VisualZone ...>` 호출에 `fit={card.image_fit}`와 `onFitChange={onFitChange}` 추가.

예) Cover:
```tsx
  const { card, mode, onFieldChange, onFieldFocus, focusedField, onImageRequest, onFocalChange, onFitChange } = props
```
```tsx
            <VisualZone imageUrl={card.image_url} slotKey="cover" mode={mode} focal={card.focal} fit={card.image_fit} onImageRequest={onImageRequest} onFocalChange={onFocalChange} onFitChange={onFitChange} />
```
나머지 3개도 동일 패턴(slotKey는 각 feature/statement/closing_v2 유지).

- [ ] **Step 5: editor + MidCanvas 배선**

`web/src/app/editor/[jobId]/page.tsx`의 `handleFocalUpdate` 다음에 추가:
```tsx
  const handleFitUpdate = useCallback((fit: 'cover' | 'contain') => {
    setLocalData((prev) => {
      const base = prev ?? apiData?.cardData
      if (!base) return prev
      const updatedCards = base.cards.map((card, idx) => {
        if (idx !== activeCardIdx) return card
        return { ...card, image_fit: fit }
      })
      const updated = { ...base, cards: updatedCards }
      debouncedSave(updated)
      return updated
    })
  }, [activeCardIdx, apiData, debouncedSave])
```
`<MidCanvas ... onFocalChange={handleFocalUpdate} />`에 추가: `onFitChange={handleFitUpdate}`.

`web/src/components/editor/MidCanvas.tsx`:
- `Props`에 `onFitChange?: (fit: 'cover' | 'contain') => void` 추가.
- 구조분해에 `onFitChange` 추가.
- `<CardRenderer ... onFocalChange={onFocalChange} />`에 `onFitChange={onFitChange}` 추가.

- [ ] **Step 6: 빌드 + 강제규칙 grep + 커밋**

Run: `cd web && npx tsc --noEmit` → 통과
Run: `cd web && npx vitest run` → 27 통과 유지
Run: `cd web && grep -nE "background|color:|fontSize|border" src/components/cards/skeletons/Statement.tsx src/components/cards/skeletons/Closing.tsx src/components/cards/skeletons/Cover.tsx src/components/cards/skeletons/Feature.tsx` → 0건
```bash
git add web/src/types/editor.ts web/src/components/cards/types.ts web/src/components/cards/shared/EditableImage.tsx web/src/components/cards/skin/VisualZone.tsx web/src/components/cards/skeletons/Cover.tsx web/src/components/cards/skeletons/Feature.tsx web/src/components/cards/skeletons/Statement.tsx web/src/components/cards/skeletons/Closing.tsx "web/src/app/editor/[jobId]/page.tsx" web/src/components/editor/MidCanvas.tsx
git commit -m "[WEB] 이미지 채움/전체 맞춤 토글 (Card.image_fit + contain 모드)"
```

- [ ] **Step 7: 시각 게이트 — contain 모드 캡처**

statement/closing 데모에 image_url + `image_fit: 'contain'`을 임시 주입해 캡처, contain이
이미지를 통째로(잘림 0) 보여주고 텍스트와 안 겹치는지 확인. 확인 후 임시 주입 되돌림.

---

## Self-Review

**Spec coverage:**
- 2.1 모델 A → 전체 전제 ✓
- 2.2 담긴 존 원칙 → Task 7/8 텍스트-사진 분리 + grep ✓
- 2.3 스코프(statement/closing_v2 zone, 데이터 4종 none) → Task 2 + 테스트 ✓
- 2.4 무이미지 적응 → Task 7/8 showImage 분기 ✓
- 2.5 focal 클릭 초점 + 교체 분리 → Task 1/4 ✓
- 3. 변경 면(11파일, 백엔드 0) → File Structure 표와 일치 ✓
- 5. 엣지(focal 후방호환=center, 클램프) → Task 1 테스트 ✓

**Placeholder scan:** 모든 코드 step에 실제 코드 포함, "TBD/등" 없음 ✓

**Type consistency:** `Focal{x,y}`(focal.ts) ↔ `Card.focal?{x,y}`(editor.ts) ↔ `onFocalChange:(focal:{x,y})=>void`(types.ts/MidCanvas/editor) 구조 호환 ✓. `clickToFocal`·`focalToObjectPosition`·`clampFocal` 이름이 Task 1 정의와 Task 4 사용처 일치 ✓. `getSlotType` 반환 'zone'/'none' ↔ 테스트 기대 일치 ✓.
