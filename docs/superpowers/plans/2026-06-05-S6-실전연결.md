# S6 실전 연결 — 8뼈대 출력 구현 계획 (B-3 코어)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** S6가 신규 8뼈대(cover_v2·statement·feature·process_v2·reasons·grid_v2·closing_v2·bigstat_compare)를 올바른 필드 포맷으로 출력하게 재작성한다. DEV_MOCK_LLM 경로를 단위 테스트로 검증한다.

**Architecture:** 변경면 = `backend/agents/s6_card_json.py` 한 파일(+ models.py 상수 위생, + 새 테스트). 데이터 모델·프론트·라우터·오케스트레이터 불변(`CardSlot.fields`는 자유 딕셔너리). 순서·뼈대 선택은 실제 LLM에선 AI(storyboard) 판단, mock에선 결정적 대표 시퀀스. grounding 규율(필드당 FieldValue·source·risk) 보존.

**Tech Stack:** Python · FastAPI · pydantic · pytest(asyncio). 선행 스펙 `docs/superpowers/specs/2026-06-05-S6-실전연결-design.md`.

**검증:** DEV_MOCK_LLM 단위 테스트(template_type=신규 8키, 필드 포맷, card_count 시퀀스, grounding 보존)가 게이트. 시각은 슬라이스2에서 이미 입증(동일 template_type·필드 포맷). 실제 LLM e2e는 비목표(후속).

**비목표:** 방법론·리뷰 아키타입, 실제 LLM 프롬프트 튜닝, 구 12 섬/`_v2` 키 정리, 사용자 재배열 UI(B-3b 별도), 막대별 개별 grounding.

---

## File Structure
**수정:**
- `backend/agents/s6_card_json.py` — `_TEMPLATE_SPEC`·`_SEQUENCING_RULES`·`_SYSTEM`·`_MOCK_TEMPLATES`·`_MOCK_FIELDS`·`_build_mock_card_data` 교체. (`_build_card_editor_data`·`_post_process`·`_count_risk`·`_iter_field_values`·`execute`는 불변.)
- `backend/core/models.py` — `VALID_TEMPLATE_TYPES`에 신규 8키 추가(위생).
**생성:**
- `backend/tests/test_s6_skeletons.py` — DEV_MOCK S6 출력 단위 테스트.

---

## Task 1: VALID_TEMPLATE_TYPES 갱신 + 실패 테스트 (TDD)

**Files:** Modify `backend/core/models.py`; Create `backend/tests/test_s6_skeletons.py`

- [ ] **Step 1: VALID_TEMPLATE_TYPES에 신규 키 추가**

`backend/core/models.py`의 `VALID_TEMPLATE_TYPES` 집합(현재 구 12키)에 신규 8키를 추가한다. 기존 12키는 남긴다(섬 철거 전 공존):

```python
VALID_TEMPLATE_TYPES = {
    # 구 12 섬 템플릿 (섬 철거 슬라이스 전까지 공존)
    "cover", "hook", "problem", "circle3", "compare2",
    "grid4", "definition", "flow", "data", "showcase",
    "closing", "brand",
    # 신규 8 뼈대 (디자인 시스템)
    "cover_v2", "statement", "feature", "process_v2",
    "reasons", "grid_v2", "closing_v2", "bigstat_compare",
}
```

- [ ] **Step 2: 실패 테스트 작성**

`backend/tests/test_s6_skeletons.py`:

```python
"""S6 DEV_MOCK 출력이 신규 8뼈대 계약을 따르는지 검증."""
from __future__ import annotations

import pathlib
import sys

import pytest

sys.path.insert(0, str(pathlib.Path(__file__).parents[2]))

from backend.core import config
from backend.core.models import S6Input, PaperMetadata, VALID_TEMPLATE_TYPES
from backend.agents.s6_card_json import s6_agent

NEW_SKELETONS = {
    "cover_v2", "statement", "feature", "process_v2",
    "reasons", "grid_v2", "closing_v2", "bigstat_compare",
}


def _mk_input(card_count: int) -> S6Input:
    return S6Input(
        job_id="test",
        section_map={"Abstract": "셀룰로스 COF 복합 미세구슬", "Results": "238 MPa"},
        page_map={1: "p1"},
        paper_metadata=PaperMetadata(title="셀룰로스 미세구슬", authors=["이호익"], year=2024),
        card_count=card_count,
    )


@pytest.fixture(autouse=True)
def _force_mock(monkeypatch):
    monkeypatch.setattr(config.settings, "DEV_MOCK_LLM", True)


@pytest.mark.asyncio
async def test_mock_uses_new_skeletons():
    out = await s6_agent.execute(_mk_input(5))
    tts = [c.template_type for c in out.card_data.cards]
    assert all(t in NEW_SKELETONS for t in tts), f"구 템플릿 누출: {tts}"
    assert tts[0] == "cover_v2"
    assert tts[-1] == "closing_v2"
    assert "bigstat_compare" in tts


@pytest.mark.asyncio
@pytest.mark.parametrize("n", [3, 4, 5, 6, 7])
async def test_card_count_respected(n):
    out = await s6_agent.execute(_mk_input(n))
    assert len(out.card_data.cards) == n
    assert out.card_data.cards[0].template_type == "cover_v2"
    assert out.card_data.cards[-1].template_type == "closing_v2"


@pytest.mark.asyncio
async def test_field_formats():
    out = await s6_agent.execute(_mk_input(7))
    by_type = {c.template_type: c.fields for c in out.card_data.cards}

    # bigstat_compare: bars = "label:value:isPrimary|..."
    bars = by_type["bigstat_compare"]["bars"].value
    rows = bars.split("|")
    assert len(rows) >= 2
    for r in rows:
        parts = r.split(":")
        assert len(parts) == 3 and parts[2] in ("0", "1"), f"bars 포맷 위반: {r}"
    assert any(r.split(":")[2] == "1" for r in rows), "강조행(1) 없음"

    # process_v2: steps = "a|b|c"
    assert "|" in by_type["process_v2"]["steps"].value

    # grid_v2: items = "label:sub|..."
    for item in by_type["grid_v2"]["items"].value.split("|"):
        assert ":" in item, f"items 포맷 위반: {item}"

    # headline 강조 마커
    assert "*" in by_type["bigstat_compare"]["headline"].value


@pytest.mark.asyncio
async def test_grounding_preserved():
    """정량 필드(bars/stat_value)에 source가 붙고 risk가 판정되는지."""
    out = await s6_agent.execute(_mk_input(7))
    by_type = {c.template_type: c.fields for c in out.card_data.cards}
    stat = by_type["bigstat_compare"]["stat_value"]
    assert stat.source is not None
    assert stat.risk_level is not None
    # 모든 FieldValue가 verified=False (코드가 검증, LLM 자칭 금지)
    for c in out.card_data.cards:
        for fv in c.fields.values():
            assert fv.verified is False
```

- [ ] **Step 3: 실패 확인**

Run (PowerShell, repo root): `python -m pytest backend/tests/test_s6_skeletons.py -q`
Expected: FAIL — 현재 mock은 구 템플릿("cover"·"closing"…)을 내므로 `test_mock_uses_new_skeletons` 등이 실패. (구 템플릿 누출 메시지 확인.)

- [ ] **Step 4: 커밋(빨간 테스트 + 상수)**

```
git add backend/core/models.py backend/tests/test_s6_skeletons.py
git commit -m "[BE] S6 8뼈대 계약 테스트(실패) + VALID_TEMPLATE_TYPES 신규 키"
```

**Rules:** models.py는 집합에 8키 추가만. 테스트 파일만 신규. 그 외 미접촉. `pytest-asyncio`가 설치돼 있어야 함 — 없으면 BLOCKED 보고(설치 여부는 Task 진행자가 `python -m pytest --version`로 확인, asyncio 마커 미인식 시 `pip install pytest-asyncio` 필요).

---

## Task 2: _MOCK 재작성 (8뼈대) → 테스트 통과

**Files:** Modify `backend/agents/s6_card_json.py`

`_MOCK_TEMPLATES`·`_MOCK_FIELDS`·`_build_mock_card_data`를 신규 8뼈대로 교체. 콘텐츠는 프론트 데모(셀룰로스-COF, mockData 카드 13~20)와 미러. 순서는 card_count별 결정적 대표 시퀀스.

- [ ] **Step 1: _MOCK_TEMPLATES 교체**

기존 `_MOCK_TEMPLATES` 리스트를 삭제하고 아래로 교체:

```python
# card_count → 대표 시퀀스 (mock 결정적; 실제 LLM은 AI가 storyboard로 선택).
# 아크 정합: 첫=cover_v2, 끝=closing_v2, bigstat_compare 포함.
_MOCK_SEQUENCES: dict[int, list[str]] = {
    3: ["cover_v2", "bigstat_compare", "closing_v2"],
    4: ["cover_v2", "feature", "bigstat_compare", "closing_v2"],
    5: ["cover_v2", "statement", "feature", "bigstat_compare", "closing_v2"],
    6: ["cover_v2", "statement", "feature", "bigstat_compare", "grid_v2", "closing_v2"],
    7: ["cover_v2", "statement", "feature", "process_v2", "bigstat_compare", "grid_v2", "closing_v2"],
}


def _mock_sequence(card_count: int) -> list[str]:
    if card_count in _MOCK_SEQUENCES:
        return _MOCK_SEQUENCES[card_count]
    # 범위 밖(이론상 3~7만 들어옴) — 7장 시퀀스를 잘라/늘려 안전 처리
    base = _MOCK_SEQUENCES[7]
    if card_count < 3:
        return _MOCK_SEQUENCES[3]
    if card_count <= 7:
        # cover 유지 + 중간 채우고 closing 유지
        mid = base[1:-1][: card_count - 2]
        return ["cover_v2", *mid, "closing_v2"]
    return base
```

- [ ] **Step 2: _MOCK_FIELDS 교체**

기존 `_MOCK_FIELDS` 딕셔너리 전체를 신규 8뼈대 필드로 교체(셀룰로스-COF):

```python
_MOCK_FIELDS: dict[str, dict[str, FieldValue]] = {
    "cover_v2": {
        "eyebrow":  _make_fv("KITECH Research Note · 2026", quality="normalized"),
        "headline": _make_fv("플라스틱을 *대체*하는 셀룰로스 미세구슬"),
        "subtitle": _make_fv("COF 복합화로 강도와 생분해성을 동시에", section="abstract", page=1),
        "org":      _make_fv("KITECH 친환경소재연구부문"),
    },
    "statement": {
        "eyebrow":  _make_fv("문제 제기"),
        "headline": _make_fv("왜 친환경 플라스틱은 *약할*까?", section="introduction", page=2),
        "body":     _make_fv(
            "생분해성 소재는 대개 강도가 낮아 구조재로 쓰기 어렵다. 이 한계가 상용화를 막아 왔다.",
            section="introduction", page=2, confidence="medium",
        ),
    },
    "feature": {
        "eyebrow":  _make_fv("핵심 혁신"),
        "headline": _make_fv("셀룰로스에 *COF*를 짜 넣다", section="methods", page=4),
        "body":     _make_fv(
            "공유결합 유기골격체(COF)를 셀룰로스 매트릭스에 복합해, 미세구슬 형태로 강도와 다공성을 함께 얻었다.",
            section="methods", page=4,
        ),
    },
    "process_v2": {
        "eyebrow":  _make_fv("제작 방법"),
        "headline": _make_fv("세 단계로 만든다", section="methods", page=5),
        "steps":    _make_fv(
            "셀룰로스 용액에 COF 전구체 분산|에멀전법으로 미세구슬 성형|동결건조로 다공 구조 고정",
            section="methods", page=5,
        ),
        "caption":  _make_fv("전 과정 무용매·저온 — 친환경 공정", confidence="medium"),
    },
    "bigstat_compare": {
        "eyebrow":      _make_fv("성능 검증"),
        "headline":     _make_fv("기존 플라스틱보다 *더 단단*하다"),
        "stat_value":   _make_fv("238", section="results", page=7,
                                 quality="exact", claim="quantitative"),
        "stat_unit":    _make_fv("MPa"),
        "stat_caption": _make_fv("셀룰로스–COF 복합 미세구슬의 압축 강도", section="results", page=7),
        "bars":         _make_fv(
            "우리 복합 구슬:238:1|폴리프로필렌(PP):199:0|무보강 셀룰로스:142:0",
            section="results", page=7, quality="exact", claim="quantitative",
        ),
        "source_ref":   _make_fv("출처: Cellulose (2024) · Results", section="results", page=7),
    },
    "reasons": {
        "eyebrow":  _make_fv("왜 이 소재인가"),
        "headline": _make_fv("셀룰로스를 고른 *세 가지* 이유"),
        "reasons":  _make_fv(
            "풍부함:지구상 가장 많은 천연 고분자, 원료 걱정이 없다"
            "|생분해성:토양·해양에서 완전 분해된다"
            "|기능화 용이:표면 -OH로 COF 결합이 쉽다",
            section="introduction", page=3,
        ),
    },
    "grid_v2": {
        "eyebrow":  _make_fv("응용 분야"),
        "headline": _make_fv("어디에 *쓰일까*?"),
        "items":    _make_fv(
            "포장재:일회용 플라스틱 대체|흡착소재:중금속·염료 제거"
            "|약물전달:다공성 캡슐|단열재:경량 구조재",
            section="discussion", page=10,
        ),
        "body":     _make_fv("강도·다공성·생분해성의 조합이 응용 폭을 넓힌다.", confidence="medium"),
    },
    "closing_v2": {
        "eyebrow":    _make_fv("맺음말"),
        "headline":   _make_fv("다음은 *대량 생산* 검증", section="conclusion", page=12),
        "body":       _make_fv(
            "실험실 성과를 파일럿 규모로 확장해 경제성과 균일도를 확인하는 후속 연구를 진행한다.",
            section="conclusion", page=12,
        ),
        "source_ref": _make_fv("출처: Cellulose (2024) · Conclusion", section="conclusion", page=12),
    },
}
```

- [ ] **Step 3: _build_mock_card_data 교체**

`sequence = _MOCK_TEMPLATES[:card_count]` 및 그 아래 closing 보정 로직을 신규 시퀀스로 교체. `_ROLE_MAP`도 신규 키로 교체. 함수 전체를 아래로 교체:

```python
def _build_mock_card_data(
    card_count: int,
    section_map: dict[str, str],
    paper_metadata,
) -> CardEditorData:
    """DEV_MOCK_LLM 모드용 — 신규 8뼈대 결정적 시퀀스."""
    title = getattr(paper_metadata, "title", "") or "연구 성과"
    authors = getattr(paper_metadata, "authors", []) or []
    year = getattr(paper_metadata, "year", 2024) or 2024
    fv = _make_fv

    meta = CardMeta(
        org=fv("한국생산기술연구원"),
        dept=fv("친환경소재연구부문"),
        researcher=fv(authors[0] if authors else "연구팀"),
        month=fv(f"{year}-01", quality="normalized"),
        edition_number=fv(f"{year}-01호", quality="normalized"),
    )

    sequence = _mock_sequence(card_count)

    cards = [
        CardSlot(
            card_num=i + 1,
            template_type=tmpl,
            fields=_MOCK_FIELDS.get(tmpl, {"headline": fv(title)}),
        )
        for i, tmpl in enumerate(sequence)
    ]

    _ROLE_MAP = {
        "cover_v2": "논문 주제 표지",
        "statement": "문제 제기 / 기존 한계",
        "feature": "핵심 혁신 소개",
        "process_v2": "제작 방법 단계",
        "bigstat_compare": "핵심 성능 + 기존 대비 + 출처",
        "reasons": "왜 이 소재인가",
        "grid_v2": "응용 분야",
        "closing_v2": "마무리 / 협력",
    }
    storyboard = Storyboard(
        story_arc=f"{title}의 배경·혁신·성능·응용을 {len(cards)}장으로 구성",
        beats=[
            CardStorybeat(
                card_num=i + 1,
                template_type=tmpl,
                narrative_role=_ROLE_MAP.get(tmpl, "내용 전달"),
                key_message=_MOCK_FIELDS.get(tmpl, {}).get(
                    "headline", fv(title)
                ).value,
            )
            for i, tmpl in enumerate(sequence)
        ],
    )

    return CardEditorData(
        storyboard=storyboard, meta=meta, cards=cards,
        theme=THEME_PRESETS["forest_green"],
        recommended_theme_key="forest_green",
    )
```

> 주: `*강조*` 마커가 들어간 headline은 `_make_fv` 기본 claim=qualitative라 risk LOW. bars/stat_value는 claim="quantitative"+exact라 _post_process에서 LOW(정상). theme는 소재/화학이라 forest_green(단, 신규 뼈대 렌더엔 영향 없음 — §스펙).

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest backend/tests/test_s6_skeletons.py -q`
Expected: PASS (전체).

- [ ] **Step 5: 회귀 — 기존 테스트**

Run: `python -m pytest backend/tests -q`
Expected: 기존 통과 테스트가 깨지지 않음(특히 conftest mock_card_data는 S7용 구 템플릿이라 무관). 새 실패 시 원인 보고.

- [ ] **Step 6: 커밋**

```
git add backend/agents/s6_card_json.py
git commit -m "[BE] S6 mock 8뼈대 재작성 — 셀룰로스-COF, card_count 시퀀스"
```

**Rules:** s6_card_json.py만. `_build_card_editor_data`·`_post_process`·`execute` 등 LLM 경로는 이 태스크에서 건드리지 않는다(Task 3·4). import 추가 불필요(THEME_PRESETS·CardStorybeat·Storyboard 이미 import됨).

---

## Task 3: _TEMPLATE_SPEC 재작성 (실제 LLM용 프롬프트)

**Files:** Modify `backend/agents/s6_card_json.py`

mock은 Task 2로 검증되지만, 실제 LLM은 `_TEMPLATE_SPEC`을 본다. 구 12 템플릿 명세를 신규 8뼈대 + 정확한 필드 포맷으로 교체.

- [ ] **Step 1: _TEMPLATE_SPEC 교체**

기존 `_TEMPLATE_SPEC` 문자열 전체를 교체:

```python
_TEMPLATE_SPEC = """
[cover_v2]    표지 (첫 카드)
  필드: eyebrow(시리즈/권호 한 줄), headline(핵심 주제, 강조어를 *별표*로 감쌈),
        subtitle(한 줄 부제), org(기관명)

[statement]   문제 제기 / 기존 한계
  필드: eyebrow(섹션 라벨), headline(독자를 끄는 큰 질문, *별표* 강조),
        body(문제·한계 2~3문장)

[feature]     핵심 혁신
  필드: eyebrow, headline(혁신 한 줄, *별표* 강조), body(혁신 설명 2~3문장)

[process_v2]  제작 방법
  필드: eyebrow, headline(공정 제목),
        steps("단계1|단계2|단계3" — |로 구분, 3~5단계), caption(보조 한 줄)

[bigstat_compare]  핵심 성능 + 기존 대비 + 출처
  필드: eyebrow, headline(*별표* 강조),
        stat_value(대표 수치 숫자만), stat_unit(단위), stat_caption(수치 맥락 한 줄),
        bars("라벨:값:강조" — |로 항목 구분, :로 쌍 구분, 강조는 1(우리/최고) 또는 0; 2~4행),
        source_ref(출처 — "출처: 저널(연도) · 섹션")

[reasons]     왜 이 소재인가
  필드: eyebrow, headline(*별표* 강조),
        reasons("제목:본문|제목:본문" — |로 항목, :로 제목·본문 구분; 2~4개)

[grid_v2]     응용 분야
  필드: eyebrow, headline(*별표* 강조),
        items("라벨:서브|라벨:서브" — |로 항목, :로 라벨·서브 구분; 2~4개),
        body(선택 요약 한 줄)

[closing_v2]  마무리 / 협력 (마지막 카드)
  필드: eyebrow, headline(*별표* 강조), body(마무리 2~3문장), source_ref(선택)

[포맷 규칙 — 엄수]
- headline 강조: 핵심어를 *별표*로 감싼다 (예: 기존보다 *더 단단*하다). 1곳 권장.
- 다항목 필드: 항목 구분 |, 쌍 구분 :. 라벨/값 내부에 |·: 사용 금지.
- bars 3번째 토큰은 1(강조행, 보통 최댓값/우리 방식) 또는 0.
- 수치(stat_value·bars)는 원문에서만, source 필수.
"""
```

- [ ] **Step 2: 타입/구문 확인 (import·실행 시 파싱)**

Run: `python -c "import backend.agents.s6_card_json"` (repo root)
Expected: import 성공(구문 오류 없음).

- [ ] **Step 3: mock 테스트 재확인(회귀)**

Run: `python -m pytest backend/tests/test_s6_skeletons.py -q`
Expected: 여전히 PASS(프롬프트 변경은 mock에 영향 없음).

- [ ] **Step 4: 커밋**

```
git add backend/agents/s6_card_json.py
git commit -m "[BE] S6 _TEMPLATE_SPEC 8뼈대 + 필드 포맷 규칙"
```

**Rules:** `_TEMPLATE_SPEC` 문자열만 교체. 다른 부분 불변.

---

## Task 4: _SEQUENCING_RULES + _SYSTEM 갱신 (아크 가이드 + 포맷 규칙)

**Files:** Modify `backend/agents/s6_card_json.py`

- [ ] **Step 1: _SEQUENCING_RULES 교체**

기존 문자열 전체 교체:

```python
_SEQUENCING_RULES = """
실험형 논문 서사 가이드 (soft — 강제 표가 아니라 AI가 논문·card_count에 맞게 취사):
- 첫 카드: [cover_v2] (표지)
- 마지막 카드: [closing_v2] (마무리)
- 가능하면 포함: [feature](혁신), [bigstat_compare](핵심 성능 — KITECH 성능 중심)
- 선택(논문에 있으면): [statement](훅/한계), [reasons](왜 이 소재),
  [process_v2](제작 단계), [grid_v2](응용)
- card_count 장에 맞게 storyboard에서 뼈대 조합을 직접 결정한다.
- 같은 뼈대 연속·중복 지양. 내러티브: 표지 → 문제/혁신 → 성능 → 응용 → 마무리.
- 8뼈대 모두 선택지다. 논문 내용이 있으면 reasons·process_v2·grid_v2도 적극 활용한다.
"""
```

- [ ] **Step 2: _SYSTEM에 포맷 규칙 추가**

`_SYSTEM` 문자열에서 `recommended_theme` 설명 블록 **앞**에 다음 단락을 삽입(가독성 원칙 A~D 아래, 핵심 원칙 위 또는 인접한 자연스러운 위치):

```
필드 포맷 규칙 (신규 뼈대):
- headline의 핵심어는 *별표*로 감싼다 (렌더 시 강조색). 예: 기존보다 *더 단단*하다
- 다항목 필드(bars·steps·reasons·items)는 항목 구분 |, 쌍 구분 :를 쓴다.
  · bars: "라벨:값:강조(1/0)"  · steps: "단계1|단계2|단계3"
  · reasons: "제목:본문|..."   · items: "라벨:서브|..."
- 라벨/값 내부에 | 또는 : 를 쓰지 않는다.
```

(나머지 _SYSTEM 내용 — 페르소나·가독성 A~D·grounding 핵심 원칙·작성 순서·FieldValue 구조·recommended_theme·반환 형식 — 그대로 유지.)

- [ ] **Step 3: import + mock 회귀**

Run: `python -c "import backend.agents.s6_card_json"` → 성공.
Run: `python -m pytest backend/tests/test_s6_skeletons.py -q` → PASS.

- [ ] **Step 4: 커밋**

```
git add backend/agents/s6_card_json.py
git commit -m "[BE] S6 시퀀싱 가이드(AI 선택) + 시스템 포맷 규칙"
```

**Rules:** 두 문자열만 수정. 가독성·grounding·recommended_theme 블록 보존.

---

## Task 5: 검증 게이트 + 최종 회귀

**Files:** 없음(검증 전용).

- [ ] **Step 1: S6 단위 테스트 전체**

Run: `python -m pytest backend/tests/test_s6_skeletons.py -v`
Expected: 모든 케이스 PASS(8뼈대·card_count 3~7·필드 포맷·grounding).

- [ ] **Step 2: 백엔드 전체 회귀**

Run: `python -m pytest backend/tests -q`
Expected: 기존 + 신규 통과. 실패 시 원인 분석(신규 코드 vs 선재 환경 이슈 구분).

- [ ] **Step 3: (선택) e2e 시각 — 백엔드 가동 시**

`DEV_MOCK_LLM=True`로 백엔드 기동 → 테스트 PDF 업로드(card_count=7) → 생성된 jobId로
`localhost:3000/render/{jobId}/1`~`/7` 또는 `/editor/{jobId}` 확인.
신규 8뼈대가 셀룰로스-COF 내용으로 렌더되는지(흰박스 0·딥틸·골드강조).
(백엔드 미가동이면 슬라이스2에서 이미 동일 template_type·필드 포맷 시각 입증됨 — 단위 테스트로 갈음.)

- [ ] **Step 4: 마무리**

미세 조정 있으면 커밋. 없으면 종료. B-3b(에디터 재배열 + 라벨)는 별도 플랜.

---

## Self-Review (작성자 체크)

**스펙 커버리지:** §2 필드 계약 → Task 2(mock)·3(_TEMPLATE_SPEC)·4(_SYSTEM)가 동일 키·구분자 사용. ✅ §3 AI 선택/soft 가이드 → Task 4 _SEQUENCING_RULES, mock은 결정적 시퀀스(Task 2). ✅ §4 변경 범위(한 파일+상수) → Task 1~4. ✅ §5 DEV_MOCK 검증 → Task 1·2·5. ✅ §6 비목표(재배열·실LLM·섬철거) 제외. ✅

**플레이스홀더 스캔:** 모든 스텝 실제 코드/명령. TBD 없음. ✅

**타입/이름 일관성:**
- 신규 8키가 Task1(VALID/테스트)·2(mock)·3(spec)·4(가이드) 전부 동일 철자. ✅
- 필드명·구분자(bars `label:value:isPrimary`, steps `|`, reasons/items `title:body`/`label:sub`, headline `*em*`)가 테스트(Task1)·mock(Task2)·spec(Task3)·system(Task4)에서 일치 — 프론트 파서(parseCompareRows/parsePairs/parsePipe/parseEmphasis)와도 일치. ✅
- `_mock_sequence`/`_MOCK_SEQUENCES`/`_MOCK_FIELDS`/`_build_mock_card_data` 이름 일관. ✅
- `_make_fv` 시그니처(quality/claim/section/page/confidence)는 기존 그대로 사용. ✅

**리스크 대응:** 필드 포맷 결합 → 테스트가 포맷 직접 검증(Task1 test_field_formats). grounding → test_grounding_preserved. 회귀 → Task2 Step5·Task5 Step2.
