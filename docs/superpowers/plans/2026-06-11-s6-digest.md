# S6 논문이해팀(다이제스트) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Architect(Sonnet) 앞에 Understand(Haiku) 다이제스트 단계를 삽입해, Sonnet이 raw 50k 대신 작은 구조화 다이제스트를 읽게 하고(비용↓) 내용모양 인벤토리로 레이아웃·grounding 재료를 명시화한다.

**Architecture:** S6-내부 멀티에이전트에 Understand 모듈 추가. `Understand(Haiku) → Architect(Sonnet, digest) → Writer(Haiku, raw+digest) → 루프 → Verify(code)`. 계약 `S6Input→S6Output` 동결. Understand 실패 시 Architect가 raw로 폴백(하드 의존 아님). 다이제스트는 힌트, raw가 grounding 권위.

**Tech Stack:** Python 3.10, FastAPI, pydantic v2, anthropic SDK, pytest + pytest-asyncio. 기존 `backend/agents/s6/` 패키지.

선행 슬라이스: `docs/superpowers/specs/2026-06-10-multiagent-s6-design.md`. 본 설계: `docs/superpowers/specs/2026-06-11-s6-digest-design.md`.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `backend/core/models.py` | PaperDigest + 서브모델, UnderstandInput, Architect/WriterInput.digest | 수정 |
| `backend/agents/s6/_util.py` | `format_digest(digest)` 다이제스트→텍스트 직렬화 | 수정 |
| `backend/agents/s6/prompts.py` | UNDERSTAND_SYSTEM/USER, WRITER_USER digest 슬롯 | 수정 |
| `backend/agents/s6/understand.py` | **NEW** Understand 모듈(Haiku) | 생성 |
| `backend/agents/s6/architect.py` | digest 모드(있으면 digest, 없으면 section_map) | 수정 |
| `backend/agents/s6/writer.py` | digest 힌트 블록(있으면 주입) | 수정 |
| `backend/agents/s6_card_json.py` | 코디네이터: understand→architect→writer, Understand 실패 폴백 | 수정 |
| `backend/scripts/s6_gate.py` | `ab` 모드(digest ON/OFF 비교) | 수정 |
| `docs/05_agent_design.md`, `docs/04_architecture.md` | Understand 단계 반영 | 수정 |
| `backend/tests/test_s6_understand.py` | Understand 단위 | 생성 |
| `backend/tests/test_s6_architect.py` 등 | digest 모드·폴백·코디네이터 배선 | 수정 |

테스트 실행은 항상 리포 루트에서: `python -m pytest backend/tests -q`.

---

## Task 1: 계약 타입 (PaperDigest + Understand/Architect/Writer 확장)

**Files:**
- Modify: `backend/core/models.py` (S6 내부 계약 섹션, `WriterOutput` 다음)
- Test: `backend/tests/test_s6_digest_contracts.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `backend/tests/test_s6_digest_contracts.py`:
```python
"""다이제스트 계약 타입 (Task 1)."""
from __future__ import annotations
import pathlib, sys
sys.path.insert(0, str(pathlib.Path(__file__).parents[2]))

from backend.core.models import (
    PaperDigest, DigestNumber, DigestComparison, DigestTerm, DigestFigure, DigestClaim,
    UnderstandInput, ArchitectInput, WriterInput, PaperMetadata, FieldSource, Storyboard,
)


def test_empty_digest_defaults():
    d = PaperDigest(one_liner="요약")
    assert d.numbers == [] and d.comparisons == [] and d.terms == []
    assert d.figures == [] and d.process_steps == [] and d.claims == []
    assert d.domain_hint == ""


def test_digest_number_carries_source():
    n = DigestNumber(value="238", unit="MPa", label="압축강도", context="우리 소재",
                     source=FieldSource(section="Results", page=7))
    assert n.source.page == 7


def test_understand_input():
    ui = UnderstandInput(section_map={"Abstract": "x"},
                         paper_metadata=PaperMetadata(title="t", year=2024, doi=None))
    assert ui.section_map["Abstract"] == "x"


def test_architect_writer_input_digest_optional():
    meta = PaperMetadata(title="t", year=2024, doi=None)
    ai = ArchitectInput(section_map={}, paper_metadata=meta, card_count=5)
    assert ai.digest is None
    wi = WriterInput(section_map={}, paper_metadata=meta, storyboard=Storyboard(story_arc="a", beats=[]))
    assert wi.digest is None
    d = PaperDigest(one_liner="요약")
    assert ArchitectInput(section_map={}, paper_metadata=meta, card_count=5, digest=d).digest is d
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_digest_contracts.py -q`
Expected: ImportError (PaperDigest 등 없음)

- [ ] **Step 3: 구현**

`backend/core/models.py` — `class WriterOutput(...)` 정의 **다음**에 추가:
```python
# ---------------------------------------------------------------------------
# 논문이해팀 Understand — PaperDigest(내용모양 인벤토리). 다이제스트는 힌트, raw가 권위.
# ---------------------------------------------------------------------------

class DigestNumber(BaseModel):
    value: str                 # 숫자(가능하면 단위 제외)
    unit: str = ""
    label: str = ""            # 무엇을 재는 값인지
    context: str = ""          # 한 줄 의미
    source: FieldSource = Field(default_factory=lambda: FieldSource(section="", page=0))


class DigestComparison(BaseModel):
    attribute: str             # 비교 속성 (예: 압축강도)
    items: list[str] = Field(default_factory=list)    # 비교 대상명
    values: list[str] = Field(default_factory=list)   # 대상별 값(items와 병렬)
    source: FieldSource = Field(default_factory=lambda: FieldSource(section="", page=0))


class DigestTerm(BaseModel):
    term: str
    plain: str = ""            # 일상어 풀이


class DigestFigure(BaseModel):
    ref: str                   # 예: "Figure 3"
    shows: str = ""            # 한 줄 설명


class DigestClaim(BaseModel):
    role: str                  # problem|innovation|result|application|method
    text: str
    source: FieldSource = Field(default_factory=lambda: FieldSource(section="", page=0))


class PaperDigest(BaseModel):
    one_liner: str = ""
    numbers: list[DigestNumber] = Field(default_factory=list)
    comparisons: list[DigestComparison] = Field(default_factory=list)
    terms: list[DigestTerm] = Field(default_factory=list)
    figures: list[DigestFigure] = Field(default_factory=list)
    process_steps: list[str] = Field(default_factory=list)
    claims: list[DigestClaim] = Field(default_factory=list)
    domain_hint: str = ""


class UnderstandInput(BaseModel):
    section_map: dict[str, str]
    paper_metadata: PaperMetadata
```

`backend/core/models.py` — `class ArchitectInput` 에 필드 추가:
```python
    digest: "PaperDigest | None" = None      # 있으면 digest 모드, 없으면 section_map(폴백)
```
`class WriterInput` 에 필드 추가:
```python
    digest: "PaperDigest | None" = None      # raw와 함께 힌트로
```
(주의: `ArchitectInput`/`WriterInput`은 `PaperDigest`보다 위에 정의돼 있으므로 문자열 애노테이션 `"PaperDigest | None"` 사용. 모듈 끝에 `ArchitectInput.model_rebuild()`, `WriterInput.model_rebuild()` 추가해 forward ref 해결.)

`backend/core/models.py` 맨 끝에 추가:
```python
ArchitectInput.model_rebuild()
WriterInput.model_rebuild()
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest backend/tests/test_s6_digest_contracts.py -q`
Expected: PASS (4 tests)

- [ ] **Step 5: 전체 회귀 + 커밋**

```bash
python -m pytest backend/tests -q     # 기존 + 신규 전부 green
git add backend/core/models.py backend/tests/test_s6_digest_contracts.py
git commit -m "[BE] S6다이제스트 Task1 — PaperDigest 계약 + Architect/WriterInput.digest(옵셔널)"
```

---

## Task 2: 다이제스트 직렬화 헬퍼 + 프롬프트

**Files:**
- Modify: `backend/agents/s6/_util.py` (`format_digest` 추가)
- Modify: `backend/agents/s6/prompts.py` (UNDERSTAND_SYSTEM/USER, WRITER_USER digest 슬롯)
- Test: `backend/tests/test_s6_digest_format.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `backend/tests/test_s6_digest_format.py`:
```python
"""다이제스트 직렬화 + 프롬프트 (Task 2)."""
from __future__ import annotations
import pathlib, sys
sys.path.insert(0, str(pathlib.Path(__file__).parents[2]))

from backend.agents.s6._util import format_digest
from backend.agents.s6 import prompts
from backend.core.models import (
    PaperDigest, DigestNumber, DigestComparison, DigestTerm, DigestClaim, FieldSource,
)


def _digest():
    return PaperDigest(
        one_liner="셀룰로스 미세구슬이 플라스틱보다 강하다",
        numbers=[DigestNumber(value="238", unit="MPa", label="압축강도", context="우리 소재",
                              source=FieldSource(section="Results", page=7))],
        comparisons=[DigestComparison(attribute="압축강도", items=["우리", "PP"], values=["238", "199"],
                                      source=FieldSource(section="Results", page=7))],
        terms=[DigestTerm(term="COF", plain="구멍 많은 단단한 그물")],
        claims=[DigestClaim(role="result", text="플라스틱보다 단단", source=FieldSource(section="Results", page=7))],
        domain_hint="재료/환경",
    )


def test_format_digest_includes_sections_and_sources():
    out = format_digest(_digest())
    assert "238" in out and "MPa" in out
    assert "압축강도" in out
    assert "COF" in out
    assert "Results" in out and "7" in out          # source 표기
    assert "셀룰로스 미세구슬" in out                # one_liner


def test_format_digest_empty_sections_omitted():
    out = format_digest(PaperDigest(one_liner="요약만"))
    assert "요약만" in out                            # one_liner는 항상
    # 빈 섹션은 잡음 없이 생략(헤더만 나열되지 않게)
    assert "numbers" not in out.lower()


def test_prompts_understand_and_writer_digest_slot():
    assert "지어내지" in prompts.UNDERSTAND_SYSTEM      # 추출 전용·환각 금지
    assert "{digest_hints}" in prompts.WRITER_USER      # Writer에 힌트 슬롯
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_digest_format.py -q`
Expected: ImportError (format_digest 없음)

- [ ] **Step 3: 구현 — `format_digest`**

`backend/agents/s6/_util.py` 끝에 추가:
```python
def format_digest(digest) -> str:
    """PaperDigest를 프롬프트용 '내용모양 인벤토리' 텍스트로 직렬화. 빈 섹션은 생략."""
    def _src(s):
        return f"[{s.section} p{s.page}]" if s and s.section else ""

    parts: list[str] = [f"한 줄 요약: {digest.one_liner}"]
    if digest.numbers:
        lines = [f"- {n.label or '값'}: {n.value}{n.unit} ({n.context}) {_src(n.source)}"
                 for n in digest.numbers]
        parts.append("핵심 수치:\n" + "\n".join(lines))
    if digest.comparisons:
        lines = [f"- {c.attribute}: " + " vs ".join(f"{i}={v}" for i, v in zip(c.items, c.values))
                 + f" {_src(c.source)}" for c in digest.comparisons]
        parts.append("비교쌍:\n" + "\n".join(lines))
    if digest.terms:
        parts.append("전문용어:\n" + "\n".join(f"- {t.term}: {t.plain}" for t in digest.terms))
    if digest.figures:
        parts.append("그림:\n" + "\n".join(f"- {f.ref}: {f.shows}" for f in digest.figures))
    if digest.process_steps:
        parts.append("공정 단계:\n" + "\n".join(f"- {s}" for s in digest.process_steps))
    if digest.claims:
        parts.append("역할별 핵심 주장:\n" + "\n".join(
            f"- [{c.role}] {c.text} {_src(c.source)}" for c in digest.claims))
    return "\n\n".join(parts)
```

- [ ] **Step 4: 구현 — 프롬프트**

`backend/agents/s6/prompts.py` 끝에 추가:
```python
# ===========================================================================
# 논문이해팀 Understand — 내용모양 인벤토리 추출 (Haiku)
# ===========================================================================

UNDERSTAND_SYSTEM = """당신은 학술 논문을 카드뉴스로 만들기 전, 원문에서 '내용 모양'을
구조화해 뽑아내는 분석가입니다. 본문을 쓰지 않습니다 — 재료만 추출합니다.

규칙:
- section_map(원문)에서만 추출한다. 원문에 없는 건 지어내지 않는다(빈 배열로 둔다).
- 수치·비교·주장에는 source{section, page}를 단다(원문 근거 위치).
- 상한: numbers≤8, comparisons≤4, terms≤5, figures≤4, claims≤8. 중요도 순으로.
- terms는 일반 독자가 모를 핵심 용어만(plain=일상어 한 줄 풀이).
- claims.role은 problem|innovation|result|application|method 중 하나.

반환: PaperDigest JSON만. 설명 없음."""

UNDERSTAND_USER = """## 논문 원문

{section_map_text}

---
제목: {title}
저자: {authors}
연도: {year}

---
## 지시
아래 JSON만 반환하라(없는 섹션은 []):
{{
  "one_liner": "논문 한 문장 요약",
  "numbers": [{{"value":"238","unit":"MPa","label":"압축강도","context":"우리 소재","source":{{"section":"Results","page":7}}}}],
  "comparisons": [{{"attribute":"압축강도","items":["우리","PP"],"values":["238","199"],"source":{{"section":"Results","page":7}}}}],
  "terms": [{{"term":"COF","plain":"구멍 많은 단단한 그물"}}],
  "figures": [{{"ref":"Figure 3","shows":"미세구슬 SEM 이미지"}}],
  "process_steps": ["단계1","단계2"],
  "claims": [{{"role":"result","text":"플라스틱보다 단단","source":{{"section":"Results","page":7}}}}],
  "domain_hint": "재료/환경"
}}"""
```

`backend/agents/s6/prompts.py` 의 `WRITER_USER` 안에서, `## 확정된 스토리보드` 블록 **앞**에 힌트 슬롯을 추가한다. 기존:
```
---
## 확정된 스토리보드 (설계팀)

{storyboard_text}
```
를 다음으로 교체:
```
---
## 사전 추출 힌트 (논문이해팀 — 수치·출처·용어, 참고용. 최종 근거는 위 원문)

{digest_hints}

---
## 확정된 스토리보드 (설계팀)

{storyboard_text}
```

- [ ] **Step 5: 통과 확인 + 커밋**

```bash
python -m pytest backend/tests/test_s6_digest_format.py -q   # 3 PASS
python -m pytest backend/tests -q                            # 회귀 green
git add backend/agents/s6/_util.py backend/agents/s6/prompts.py backend/tests/test_s6_digest_format.py
git commit -m "[BE] S6다이제스트 Task2 — format_digest + Understand/Writer 프롬프트 슬롯"
```

---

## Task 3: Understand 모듈 (Haiku)

**Files:**
- Create: `backend/agents/s6/understand.py`
- Test: `backend/tests/test_s6_understand.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `backend/tests/test_s6_understand.py`:
```python
"""논문이해팀 Understand 모듈 (Task 3)."""
from __future__ import annotations
import json, pathlib, sys
import pytest
sys.path.insert(0, str(pathlib.Path(__file__).parents[2]))
from unittest.mock import AsyncMock
from backend.agents.s6 import understand as und_mod
from backend.core.config import settings
from backend.core.models import PaperDigest, UnderstandInput, PaperMetadata


def _meta():
    return PaperMetadata(title="셀룰로스 미세구슬", authors=["Lee"], year=2024, doi=None)


_RAW = json.dumps({
    "one_liner": "셀룰로스가 플라스틱보다 강하다",
    "numbers": [{"value": "238", "unit": "MPa", "label": "압축강도", "context": "우리",
                 "source": {"section": "Results", "page": 7}}],
    "comparisons": [], "terms": [{"term": "COF", "plain": "단단한 그물"}],
    "figures": [], "process_steps": [], "claims": [], "domain_hint": "재료",
})


@pytest.mark.asyncio
async def test_understand_parses_digest_and_routes_to_haiku(monkeypatch):
    call = AsyncMock(return_value=_RAW)
    monkeypatch.setattr(und_mod.llm_client, "call", call)
    out = await und_mod.understand.run(UnderstandInput(section_map={"Abstract": "x"}, paper_metadata=_meta()))
    assert isinstance(out, PaperDigest)
    assert out.numbers[0].value == "238"
    assert out.numbers[0].source.page == 7
    assert out.terms[0].term == "COF"
    # Haiku 기본(모델 미지정 또는 LLM_MODEL)
    assert call.call_args.kwargs.get("model") in (None, settings.LLM_MODEL)
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_understand.py -q`
Expected: ModuleNotFoundError (understand 없음)

- [ ] **Step 3: 구현**

Create `backend/agents/s6/understand.py`:
```python
"""논문이해팀 Understand — raw section_map → PaperDigest(내용모양 인벤토리). (Haiku)

다이제스트는 힌트일 뿐(권위는 raw). Architect 입력 압축 + 레이아웃/grounding 재료 구조화.
"""
from __future__ import annotations
import json
import logging

from ...core.llm_client import llm_client
from ...core.models import PaperDigest, UnderstandInput
from ._util import extract_json, format_section_map
from . import prompts

logger = logging.getLogger(__name__)


class Understand:
    """UnderstandInput → PaperDigest. 코디네이터가 호출한다."""

    async def run(self, inp: UnderstandInput) -> PaperDigest:
        meta = inp.paper_metadata
        user = prompts.UNDERSTAND_USER.format(
            section_map_text=format_section_map(inp.section_map),
            title=meta.title or "unknown",
            authors=", ".join(meta.authors) if meta.authors else "unknown",
            year=meta.year or "unknown",
        )
        raw = await llm_client.call(
            system_prompt=prompts.UNDERSTAND_SYSTEM,
            user_prompt=user,
            max_tokens=4000,
            temperature=0.1,
            timeout_s=120,
        )
        parsed = json.loads(extract_json(raw))
        return PaperDigest.model_validate(parsed)


understand = Understand()
```

- [ ] **Step 4: 통과 확인 + 커밋**

```bash
python -m pytest backend/tests/test_s6_understand.py -q     # PASS
python -m pytest backend/tests -q                           # 회귀 green
git add backend/agents/s6/understand.py backend/tests/test_s6_understand.py
git commit -m "[BE] S6다이제스트 Task3 — 논문이해팀 Understand 모듈(Haiku)"
```

---

## Task 4: Architect digest 모드 (+ section_map 폴백 유지)

**Files:**
- Modify: `backend/agents/s6/architect.py`
- Test: `backend/tests/test_s6_architect.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성 (기존 파일에 추가)**

`backend/tests/test_s6_architect.py` 끝에 추가:
```python
@pytest.mark.asyncio
async def test_architect_uses_digest_when_present(monkeypatch):
    from backend.core.models import PaperDigest, DigestNumber, FieldSource
    beats = [_beat(1, "cover_v2"), _beat(2, "multistat"), _beat(3, "closing_v2")]
    call = _patch_llm(monkeypatch, _storyboard_json(beats))
    digest = PaperDigest(one_liner="요약",
                         numbers=[DigestNumber(value="238", unit="MPa", label="강도",
                                               source=FieldSource(section="Results", page=7))])
    await arch_mod.architect.run(ArchitectInput(
        section_map={"Abstract": "x" * 9999}, paper_metadata=_meta(), card_count=3, digest=digest))
    sent = call.call_args.kwargs["user_prompt"]
    assert "238" in sent and "MPa" in sent        # 다이제스트가 프롬프트에
    assert "x" * 9999 not in sent                 # raw 50k는 안 들어감(압축됨)


@pytest.mark.asyncio
async def test_architect_falls_back_to_section_map_without_digest(monkeypatch):
    beats = [_beat(1, "cover_v2"), _beat(2, "feature"), _beat(3, "closing_v2")]
    call = _patch_llm(monkeypatch, _storyboard_json(beats))
    await arch_mod.architect.run(ArchitectInput(
        section_map={"Abstract": "RAWTEXT_MARKER"}, paper_metadata=_meta(), card_count=3))
    assert "RAWTEXT_MARKER" in call.call_args.kwargs["user_prompt"]   # raw 직접
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_architect.py -q`
Expected: FAIL (digest가 프롬프트에 안 들어감 / 현재는 section_map만)

- [ ] **Step 3: 구현**

`backend/agents/s6/architect.py` — import에 `format_digest` 추가:
```python
from ._util import extract_json, format_section_map, format_digest
```
`run` 메서드에서 user 프롬프트 구성 부분의 `section_map_text=format_section_map(inp.section_map),` 를 다음으로 교체:
```python
            section_map_text=(format_digest(inp.digest) if inp.digest
                              else format_section_map(inp.section_map)),
```
(나머지 로직·검증·피드백루프 동일. digest가 있으면 다이제스트 텍스트가 `## 논문 원문` 슬롯에 들어간다.)

- [ ] **Step 4: 통과 확인 + 커밋**

```bash
python -m pytest backend/tests/test_s6_architect.py -q   # 기존 + 신규 2 PASS
python -m pytest backend/tests -q
git add backend/agents/s6/architect.py backend/tests/test_s6_architect.py
git commit -m "[BE] S6다이제스트 Task4 — Architect digest 모드(+section_map 폴백 유지)"
```

---

## Task 5: Writer digest 힌트 주입

**Files:**
- Modify: `backend/agents/s6/writer.py`
- Test: `backend/tests/test_s6_writer.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성 (기존 파일에 추가)**

`backend/tests/test_s6_writer.py` 끝에 추가:
```python
@pytest.mark.asyncio
async def test_writer_injects_digest_hints_and_keeps_raw(monkeypatch):
    from backend.core.models import PaperDigest, DigestNumber, FieldSource
    call = _patch(monkeypatch, _cards_json(["cover_v2", "multistat", "closing_v2"]))
    digest = PaperDigest(one_liner="요약",
                         numbers=[DigestNumber(value="238", unit="MPa", label="강도",
                                               source=FieldSource(section="Results", page=7))])
    await wr_mod.writer.run(WriterInput(
        section_map={"Results": "RAW_AUTHORITY"}, paper_metadata=_meta(),
        storyboard=_sb(["cover_v2", "multistat", "closing_v2"]), digest=digest))
    sent = call.call_args.kwargs["user_prompt"]
    assert "238" in sent                  # 다이제스트 힌트 주입
    assert "RAW_AUTHORITY" in sent        # raw도 여전히(권위)


@pytest.mark.asyncio
async def test_writer_digest_none_renders_empty_hint(monkeypatch):
    call = _patch(monkeypatch, _cards_json(["cover_v2", "multistat", "closing_v2"]))
    await wr_mod.writer.run(WriterInput(
        section_map={"Results": "RAW"}, paper_metadata=_meta(),
        storyboard=_sb(["cover_v2", "multistat", "closing_v2"])))
    sent = call.call_args.kwargs["user_prompt"]
    assert "RAW" in sent                  # digest 없어도 정상 동작
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_writer.py -q`
Expected: FAIL (KeyError: 'digest_hints' — WRITER_USER에 새 슬롯이 생겼는데 writer가 안 채움)

- [ ] **Step 3: 구현**

`backend/agents/s6/writer.py` — import에 `format_digest` 추가:
```python
from ._util import extract_json, format_section_map, format_digest
```
`run`의 `user = prompts.WRITER_USER.format(...)` 호출에 `digest_hints` 인자 추가:
```python
            digest_hints=(format_digest(inp.digest) if inp.digest else "(없음)"),
```
(나머지 동일.)

- [ ] **Step 4: 통과 확인 + 커밋**

```bash
python -m pytest backend/tests/test_s6_writer.py -q   # 기존 + 신규 2 PASS
python -m pytest backend/tests -q
git add backend/agents/s6/writer.py backend/tests/test_s6_writer.py
git commit -m "[BE] S6다이제스트 Task5 — Writer digest 힌트 주입(raw 권위 유지)"
```

---

## Task 6: 코디네이터 배선 + Understand 실패 폴백

**Files:**
- Modify: `backend/agents/s6_card_json.py`
- Test: `backend/tests/test_s6_coordinator.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성 (기존 파일에 추가)**

`backend/tests/test_s6_coordinator.py` 끝에 추가:
```python
@pytest.mark.asyncio
async def test_coordinator_runs_understand_and_passes_digest(monkeypatch):
    from backend.core.models import PaperDigest, DigestNumber, FieldSource, ArchitectInput, WriterInput
    types_ = ["cover_v2", "multistat", "closing_v2"]
    digest = PaperDigest(one_liner="요약",
                         numbers=[DigestNumber(value="238", source=FieldSource(section="Results", page=7))])
    und = AsyncMock(return_value=digest)
    arch = AsyncMock(return_value=ArchitectOutput(storyboard=_sb(types_), recommended_theme="forest_green"))
    wr = AsyncMock(return_value=WriterOutput(cards=_cards(types_), meta=_meta(), mismatch_signals=[]))
    monkeypatch.setattr(s6mod.understand, "run", und)
    monkeypatch.setattr(s6mod.architect, "run", arch)
    monkeypatch.setattr(s6mod.writer, "run", wr)

    out = await s6mod.s6_agent.execute(_input(3))
    assert und.await_count == 1
    # Architect/Writer가 digest를 받았는지
    assert arch.call_args.args[0].digest is digest
    assert wr.call_args.args[0].digest is digest
    assert out.warnings == []


@pytest.mark.asyncio
async def test_coordinator_falls_back_when_understand_fails(monkeypatch):
    from backend.core.models import ArchitectInput, WriterInput
    types_ = ["cover_v2", "feature", "closing_v2"]
    und = AsyncMock(side_effect=Exception("boom"))   # 항상 실패
    arch = AsyncMock(return_value=ArchitectOutput(storyboard=_sb(types_), recommended_theme="forest_green"))
    wr = AsyncMock(return_value=WriterOutput(cards=_cards(types_), meta=_meta(), mismatch_signals=[]))
    monkeypatch.setattr(s6mod.asyncio, "sleep", AsyncMock())   # 백오프 제거
    monkeypatch.setattr(s6mod.understand, "run", und)
    monkeypatch.setattr(s6mod.architect, "run", arch)
    monkeypatch.setattr(s6mod.writer, "run", wr)

    out = await s6mod.s6_agent.execute(_input(3))
    # degraded: digest 없이 진행, Architect는 section_map(digest None)
    assert arch.call_args.args[0].digest is None
    assert any("다이제스트 생략" in w for w in out.warnings)
    assert len(out.card_data.cards) == 3        # 결과는 정상 생성
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest backend/tests/test_s6_coordinator.py -q`
Expected: FAIL (understand 미배선, digest 미전달)

- [ ] **Step 3: 구현 — import + execute 배선**

`backend/agents/s6_card_json.py` — import에 understand + 타입 추가:
```python
from .s6.understand import understand
```
models import에 `PaperDigest, UnderstandInput` 추가(기존 import 블록에).

execute()의 비-mock 경로에서 Architect 호출 **앞**에 다이제스트 단계 삽입. 기존:
```python
        # ── 멀티에이전트: 설계팀(Architect, Sonnet) → 콘텐츠팀(Writer, Haiku) ──
        arch_out: ArchitectOutput = await self._with_retries(
            lambda: architect.run(ArchitectInput(
                section_map=input_data.section_map,
                paper_metadata=input_data.paper_metadata,
                card_count=input_data.card_count,
            )),
            stage="Architect", truncation_is_card_overload=False,
        )

        wr_out: WriterOutput = await self._with_retries(
            lambda: self._run_writer(input_data, arch_out.storyboard),
            stage="Writer", truncation_is_card_overload=True,
            card_count=input_data.card_count,
        )

        storyboard = arch_out.storyboard
        warnings: list[str] = []
```
를 다음으로 교체:
```python
        # ── 논문이해팀: raw → 다이제스트(실패 시 degraded 폴백) ──
        digest, warnings = await self._get_digest(input_data)

        # ── 설계팀(Architect, Sonnet, digest 우선) → 콘텐츠팀(Writer, Haiku, raw+digest) ──
        arch_out: ArchitectOutput = await self._with_retries(
            lambda: architect.run(ArchitectInput(
                section_map=input_data.section_map,
                paper_metadata=input_data.paper_metadata,
                card_count=input_data.card_count,
                digest=digest,
            )),
            stage="Architect", truncation_is_card_overload=False,
        )

        wr_out: WriterOutput = await self._with_retries(
            lambda: self._run_writer(input_data, arch_out.storyboard, digest=digest),
            stage="Writer", truncation_is_card_overload=True,
            card_count=input_data.card_count,
        )

        storyboard = arch_out.storyboard
```
(주의: `warnings`는 이제 `_get_digest`가 반환하므로 기존 `warnings: list[str] = []` 줄은 삭제됨. 이후 피드백 루프에서 `warnings.extend(loop_warns)` 형태가 되도록 — 기존 루프 코드는 `storyboard, wr_out, warnings = await self._resolve_mismatch(...)` 였는데, degraded 경고를 보존하려면 변경: 아래 Step 3b 참고.)

- [ ] **Step 3b: 구현 — 루프 경고 합산 + `_get_digest` + `_run_writer` digest 인자**

피드백 루프 블록을 degraded 경고 보존하도록 수정. 기존:
```python
        if wr_out.mismatch_signals:
            storyboard, wr_out, warnings = await self._resolve_mismatch(
                input_data, arch_out, wr_out,
            )
```
를:
```python
        if wr_out.mismatch_signals:
            storyboard, wr_out, loop_warns = await self._resolve_mismatch(
                input_data, arch_out, wr_out,
            )
            warnings = warnings + loop_warns
```

`_run_writer` 시그니처에 digest 추가 (메서드 정의 수정):
```python
    async def _run_writer(
        self, input_data: S6Input, storyboard: Storyboard,
        only_beats: list[int] | None = None, digest=None,
    ) -> WriterOutput:
        wr = await writer.run(WriterInput(
            section_map=input_data.section_map,
            paper_metadata=input_data.paper_metadata,
            storyboard=storyboard,
            only_beats=only_beats,
            digest=digest,
        ))
```
(나머지 커버리지 검증 동일.) 피드백 루프 안의 `_run_writer` 호출(`_resolve_mismatch` 내부)도 digest를 넘기도록: `_resolve_mismatch`는 digest를 모르므로, 간단히 부분 재작성 시 digest=None으로 둔다(부분 재작성은 raw로 충분, 힌트 없이도 동작). 즉 `_resolve_mismatch` 내부 `_run_writer(input_data, arch2.storyboard, only_beats=bad)` 는 그대로 둔다(digest 생략 = None).

`_get_digest` 메서드 추가 (execute 위, `_run_writer` 근처):
```python
    async def _get_digest(self, input_data: S6Input):
        """Understand 실행 — 실패하면 degraded 폴백(digest=None) + 경고."""
        try:
            digest = await self._with_retries(
                lambda: understand.run(UnderstandInput(
                    section_map=input_data.section_map,
                    paper_metadata=input_data.paper_metadata,
                )),
                stage="Understand", truncation_is_card_overload=False,
            )
            return digest, []
        except RuntimeError as exc:
            logger.warning("S6 Understand 실패 — 원문 직접 폴백: %s", exc)
            return None, ["다이제스트 생략—원문 직접(degraded)"]
```

- [ ] **Step 4: 통과 확인 + 커밋**

```bash
python -m pytest backend/tests/test_s6_coordinator.py -q   # 기존 + 신규 2 PASS
python -m pytest backend/tests -q                          # 전체 green
git add backend/agents/s6_card_json.py backend/tests/test_s6_coordinator.py
git commit -m "[BE] S6다이제스트 Task6 — 코디네이터 배선 + Understand 실패 degraded 폴백"
```

---

## Task 7: A/B 게이트 (digest ON/OFF 비교)

**Files:**
- Modify: `backend/scripts/s6_gate.py` (`ab` 모드)

(스크립트는 실 LLM 호출이라 자동 테스트 아님 — 수동 검증 도구. 단위 회귀는 기존 테스트가 담당.)

- [ ] **Step 1: 구현 — `ab` 모드 추가**

`backend/scripts/s6_gate.py` — `main_full` 다음에 추가:
```python
async def main_ab(card_count: int = 6) -> int:
    """A/B — 다이제스트 ON/OFF 비교(비용 프록시·확장레이아웃·CRITICAL·다이제스트 충실)."""
    from backend.agents.s6.understand import understand
    from backend.agents.s6.architect import architect
    from backend.agents.s6._util import format_digest, format_section_map
    from backend.core.models import ArchitectInput, UnderstandInput

    sm = _section_map()
    meta = PaperMetadata(title="cellulose COF microbeads", authors=["KITECH"], year=2024, doi=None)

    # OFF: Architect가 raw 직접
    off_chars = len(format_section_map(sm))
    # ON: Understand → digest → Architect 입력
    t0 = time.time()
    digest = await understand.run(UnderstandInput(section_map=sm, paper_metadata=meta))
    on_chars = len(format_digest(digest))
    und_dt = time.time() - t0

    print("\n=== A/B: 다이제스트 ON/OFF ===")
    print(f"[비용 프록시] Architect 입력 문자수  OFF(raw)={off_chars:,}  ON(digest)={on_chars:,}  "
          f"감소율={100*(1-on_chars/max(off_chars,1)):.0f}%")
    print(f"[다이제스트 충실] numbers={len(digest.numbers)} comparisons={len(digest.comparisons)} "
          f"terms={len(digest.terms)} claims={len(digest.claims)}  (Understand {und_dt:.1f}s)")

    async def _layouts(use_digest: bool):
        out = await architect.run(ArchitectInput(
            section_map=sm, paper_metadata=meta, card_count=card_count,
            digest=digest if use_digest else None))
        return [b.template_type for b in out.storyboard.beats]

    on = await _layouts(True)
    off = await _layouts(False)
    ext = lambda ts: [t for t in ts if t in EXTENDED]
    print(f"[레이아웃] ON  확장 {len(ext(on))}개: {on}")
    print(f"[레이아웃] OFF 확장 {len(ext(off))}개: {off}")

    ok = (on_chars < off_chars) and (len(digest.numbers) + len(digest.terms) > 0) and (len(ext(on)) >= len(ext(off)))
    print("\n" + ("✅ A/B 합격 — 비용↓ + 다이제스트 충실 + 레이아웃 비퇴행"
                  if ok else "❌ A/B 미달 — 위 지표 점검"))
    return 0 if ok else 1
```
그리고 `__main__` 분기에 추가:
```python
    if len(sys.argv) > 1 and sys.argv[1] == "ab":
        raise SystemExit(asyncio.run(main_ab()))
```
(기존 `full`/기본 분기 위에 추가.)

- [ ] **Step 2: 수동 실행 (실 API)**

Run: `python backend/scripts/s6_gate.py ab`
Expected: 비용 프록시 감소(ON ≪ OFF), 다이제스트 numbers/terms ≥1, ON 확장 ≥ OFF 확장 → ✅

- [ ] **Step 3: 커밋**

```bash
git add backend/scripts/s6_gate.py
git commit -m "[BE] S6다이제스트 Task7 — A/B 게이트(digest ON/OFF: 비용·충실·레이아웃)"
```

---

## Task 8: docs 갱신

**Files:**
- Modify: `docs/05_agent_design.md` (5-3 처리흐름에 Understand 추가)
- Modify: `docs/04_architecture.md` (S6 다이어그램)

- [ ] **Step 1: docs/05 — 처리흐름에 Understand 단계 추가**

`docs/05_agent_design.md` 5-3의 처리흐름 코드블록 맨 위(`설계팀 Architect` 앞)에 추가:
```
논문이해팀 Understand (Haiku) — s6/understand.py
  raw section_map → PaperDigest(내용모양 인벤토리: 수치+출처·비교·용어·그림·공정·역할별 주장).
  실패 시 degraded 폴백(Architect가 raw 직접). 다이제스트는 힌트, raw가 grounding 권위.
```
그리고 `설계팀 Architect` 줄의 입력을 "작은 다이제스트(없으면 raw 폴백)"로, `콘텐츠팀 Writer` 입력을 "raw(권위) + 다이제스트(힌트)"로 수정.

- [ ] **Step 2: docs/04 — 다이어그램**

`docs/04_architecture.md` 의 S6 노드 블록을 다음으로 교체:
```
  ├─ S6: Card News JSON        내부 멀티에이전트 (계약 S6Input→S6Output 동결)
  │        │  논문이해팀 Understand(Haiku) → 설계팀 Architect(Sonnet) → 콘텐츠팀 Writer(Haiku) → 검증(코드)
  │        │  다이제스트=힌트(raw 권위), Understand 실패 시 raw 폴백. 피드백 루프 1회.
  │        │  출력: CardEditorData(storyboard, cards: List[CardSlot])
  │        ▼
```

- [ ] **Step 3: 커밋**

```bash
git add docs/05_agent_design.md docs/04_architecture.md
git commit -m "[DOCS] S6다이제스트 Task8 — 04/05에 Understand 단계 반영"
```

---

## Task 9: 전체 회귀

- [ ] **Step 1: backend**

Run: `python -m pytest backend/tests -q`
Expected: 기존 77 + 신규(약 13) 전부 PASS

- [ ] **Step 2: web (영향 없음 확인)**

Run: `cd web && npx tsc --noEmit && npx vitest run`
Expected: tsc 0 errors, vitest 49 passed

- [ ] **Step 3: 마무리**

`superpowers:finishing-a-development-branch`로 브랜치 마무리(머지/PR 선택).

---

## Self-Review

**1. Spec coverage:**
- Understand 모듈(Haiku) → Task 3 ✓
- PaperDigest 내용모양 인벤토리 → Task 1 ✓
- Architect=digest만/Writer=digest+raw → Task 4, 5 ✓
- degraded 폴백(Understand 실패→raw) → Task 6 ✓
- 계약 동결 → 모든 Task가 S6Input/S6Output 불변 ✓
- A/B 검증(비용·grounding·충실·레이아웃) → Task 7 ✓
- docs → Task 8 ✓
- DEV_MOCK은 Understand 건너뜀 → 코디네이터 mock 분기 미변경(Task 6은 비-mock 경로만 수정) ✓

**2. Placeholder scan:** 모든 코드 스텝에 실제 코드 포함. "상위 N개"는 프롬프트에 구체 숫자(≤8/≤4/≤5/≤4/≤8) 명시. 없음.

**3. Type consistency:** `PaperDigest`/`DigestNumber`/`UnderstandInput`/`ArchitectInput.digest`/`WriterInput.digest`는 Task1에서 정의 → Task3~6에서 동일 이름 사용. `format_digest`(Task2) → Task4/5/7에서 사용. `understand.run`/`Understand`(Task3) → Task6에서 사용. `_get_digest`/`_run_writer(digest=)`(Task6) 일관. ✓
